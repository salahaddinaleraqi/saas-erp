from django.contrib.auth.models import AbstractUser
from django.db import models


class User(AbstractUser):
    """
    Custom User model for the SaaS ERP project.
    Additional fields will be added in future sprints.
    """

    pass